# Overlay Index

This public repo keeps overlays generic and public-safe.

Use overlays for environment-specific mapping that still belongs in versioned docs, for example:
- `references/hosts-inventory.md`
- `references/bot-service-map.md`

If the operator needs richer private topology, keep that in a separate private repo or local inventory, not here.
